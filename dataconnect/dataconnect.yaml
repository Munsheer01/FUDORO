specVersion: "v1"
serviceId: "fudoro"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fudoro-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
